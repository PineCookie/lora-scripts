{
  "name": "lora-scripts-frontend-src",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:preview": "node scripts/build.mjs --out dist",
    "check": "node --check src/main.js && node --check scripts/build.mjs"
  }
}
